import { FormatoRecomendacionPipe } from './formato-recomendacion.pipe';

describe('FormatoRecomendacionPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatoRecomendacionPipe();
    expect(pipe).toBeTruthy();
  });
});
